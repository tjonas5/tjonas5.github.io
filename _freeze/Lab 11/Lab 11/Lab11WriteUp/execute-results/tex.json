{
  "hash": "0ba44875be13d193042133d3885a021a",
  "result": {
    "engine": "knitr",
    "markdown": "---\ntitle: Lab Eleven -- Point Estimation and Resampling # Title of the document\n\n# Bibliography Details\nbibliography: Lab11References.bib  # Name of bib file in the same folder \nbibliographystyle: apa            # Bibliography style\n\nexecute:               # Default settings for code blocks\n  warning: false       # Do not print warnings\n  message: false       # Do not print messages\n  size: \"scriptsize\"   # Set code size to script size\n\n# Below will do html by default. To render the entire project\n# Use the following code:\n# quarto::quarto_render(\"Lab10WriteUp.qmd\")\nformat:\n  pdf:        # Specify .pdf formatting options \n    documentclass: article     # Document Class\n    number-sections: true      # Number Sections\n    reference-section-title: \"References\" # Title for reference section\n    code-line-numbers: true    # Line numbers printed in code\n    cap-location: top          # Captions at the top of tables \n    geometry:\n      - top=0.75in\n      - left=0.75in\n      - bottom=0.75in\n      - right=0.75in\n  html:       # Specify HTML formatting options \n    number-sections: true      # Number Sections\n    code-fold: true            # Button to show code\n    code-line-numbers: true    # Line numbers printed in code\n    cap-location: top          # Captions at the top of tables and figures\nformat-links: [pdf]\n---\n\n \\normalsize\n\n\n::: {.cell}\n\n:::\n\n\n \\normalsize\n\n\n\n**Tips**\n\n* Complete the tasks below. Make sure to start your solutions in on a new line that starts with \"**Solution**:\".\n* Make sure to use the Quarto Cheatsheet. This will make completing and writing up the lab *much* easier.\n* Make sure to use \"enter\" to make your code readable, so it doesn't run off the page. I switched the Quarto document to automatically render to .pdf, so you can see what you're submitting by default. You can switch back for the highlighting by moving the html block above the pdf block in the YAML header.\n* Don't forget that you can copy-and-paste code when you're doing something similar as we did in class!\n* Make sure to plot all computed probabilities. This is good `ggplot` practice AND will help make sure you don't make a mistake when computing the probability.\n\n\n# Introduction\nWhen performing point estimation, there isn't just one *right* answer. For example, the Method of Moments (MOM) and Maximum Likelihood Estimates (MLEs) aren't always the same.\n\nOne question that comes up, is why we calculate the sample variance as\n$$S_{n-1}^2 = \\frac{1}{n-1} \\sum_{i=1}^n (X_i -\\bar{X})^2,$$\nwhen it is supposed to be the ``average squared distance from the mean, i.e., \n$$S_n^2 = \\frac{1}{n} \\sum_{i=1}^n (X_i -\\bar{X})^2.$$\n\n\n# Comparing Estimators\n\n## Initial Simulation\nIn this lab, we will explore why we use $S_{n-1}^2$ and not $S_n^2$. To do this, we will generate data from four known distributions:\n\n* Poisson$(\\lambda=2)$\n* Binomial$\\left(n=50, p=\\frac{5-\\sqrt{21}}{10}\\right)$\n* Normal$(\\mu=0, \\sigma=\\sqrt{2})$\n* Exponential$\\left(\\lambda = \\frac{1}{\\sqrt{2}}\\right).$\n\n**Remark**: Note that I have selected the parameters so that the true variance for all four models is 2.\n\nFor each distribution, write a loop that completes the following 1000 times. Put `set.seed(7272)` at the top of your code chunk.\n\n* Generate a sample of size $n$ from the distribution.\n* Calculate and save $S_{n-1}^2$ and $S_n^2.$\n\nStart with $n=20$. Is there evidence that $S_{n-1}^2$ is a better estimator than $S_n^2$?\n\n**Solution** \n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(7272)\nn = 20\nn.sims=1000\n# Poisson \nlambda = 2\nresults_p = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rpois(n, lambda)\n (results_p$var[i] = var(x) ) # variance\n (results_p$not_var[i] = (n-1)/n * var(x)) # the not variance - avg. squared dist from mean\n}\nmeans_p = tibble(var_mean = mean(results_p$var), not_var_mean = mean(results_p$not_var))\n\n# Binomial\nsize = 50\np = (5-sqrt(21))/10\nresults_b = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rbinom(n, size, p)\n (results_b$var[i] = var(x) ) # variance\n (results_b$not_var[i] = (n-1)/n * var(x)) # the not variance - avg. squared dist from mean\n}\nmeans_b = tibble(var_mean = mean(results_b$var), not_var_mean = mean(results_b$not_var))\n\n# Normal\nmean = 0\nsd = sqrt(2)\nresults_n = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rnorm(n, mean, sd)\n (results_n$var[i] = var(x) ) # variance\n (results_n$not_var[i] = (n-1)/n * var(x)) # the not variance - avg. squared dist from mean\n}\nmeans_n = tibble(var_mean = mean(results_n$var), not_var_mean = mean(results_n$not_var))\n\n# Exponential\nlambda = 1/sqrt(2)\nresults_e = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rexp(n, lambda)\n (results_e$var[i] = var(x) ) # variance\n (results_e$not_var[i] = (n-1)/n * var(x)) # the \"not\" variance - avg. squared dist from mean\n}\nmeans_e = tibble(var_mean = mean(results_e$var), not_var_mean = mean(results_e$not_var))\n```\n:::\n\n\n \\normalsize\n\nSince we know the true variance of all the models is 2, we can compare the accuracy of the calculated variance and the calculated \"not-variance\" by seeing how close they are to 2. For every model, at n=20, the variance is closer to 2 than the \"not-variance\", which shows that it is a better estimator. \n\nCreate a plot or combination of plots (using patchwork) to show the results across all four distributions. Make sure to create a corresponding table that numerically summarizes the results of the simulation. You should include\n\\begin{align*}\n\\textrm{Bias} &= \\textrm{Average of Estimates} - \\textrm{True Value}\\\\\n\\textrm{Precision} &= \\frac{1}{\\textrm{Variance of Estimates}}\\\\\n\\textrm{Mean Square Error} &= \\textrm{Bias}^2 + \\textrm{Variance of Estimates}.\n\\end{align*}\n\n**Solution**\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\n# biases\nbias_b = tibble(varbias = mean(results_b$var) - 2, notvarbias=mean(results_b$not_var) - 2)\nbias_p = tibble(varbias = mean(results_p$var) - 2, notvarbias=mean(results_p$not_var) - 2)\nbias_n = tibble(varbias = mean(results_n$var) - 2, notvarbias=mean(results_n$not_var) - 2)\nbias_e = tibble(varbias = mean(results_e$var) - 2, notvarbias=mean(results_e$not_var) - 2)\n\n# precisions\nprecision_b = tibble(var_prec = 1/var(results_b$var), notvar_prec = 1/var(results_b$not_var))\nprecision_p = tibble(var_prec = 1/var(results_p$var), notvar_prec = 1/var(results_p$not_var))\nprecision_n = tibble(var_prec = 1/var(results_n$var), notvar_prec = 1/var(results_n$not_var))\nprecision_e = tibble(var_prec = 1/var(results_e$var), notvar_prec = 1/var(results_e$not_var))\n\n# MSE\nmse_b = tibble(var_mse = (bias_b$varbias)^2 + var(results_b$var),\nvar_notmse = bias_b$notvarbias)^2 + var(results_b$not_var)\n\nmse_p = tibble(var_mse = (bias_p$varbias)^2 + var(results_p$var),\nvar_notmse = bias_p$notvarbias)^2 + var(results_p$not_var)\n\nmse_n = tibble(var_mse = (bias_n$varbias)^2 + var(results_n$var),\nvar_notmse = (bias_n$notvarbias)^2 + var(results_n$not_var))\n\nmse_e = tibble(var_mse = (bias_e$varbias)^2 + var(results_e$var),\nvar_notmse = (bias_e$notvarbias)^2 + var(results_e$not_var))\n\n\n# combining all calculated values into a tibble for each distrib\ncombined_p = bind_cols(mse_p, bias_p, precision_p, means_p)\ncombined_b = bind_cols(mse_b, bias_b, precision_b, means_b)\ncombined_e = bind_cols(mse_e, bias_e, precision_e, means_e)\ncombined_n = bind_cols(mse_n, bias_n, precision_n, means_n)\n\nall_dists = bind_rows(combined_p, combined_b, combined_n, combined_e) |>\n  add_column(Distribution = c(\"Poisson\", \"Binomial\", \"Normal\", \"Exponential\"), .before = 1)\n```\n:::\n\n\n \\normalsize\n\n\n \\scriptsize\n\n\n::: {#tbl-dists-compared-n20 .cell tbl-cap='Different properties of the distributions' tbl-align='center'}\n\n```{.r .cell-code}\nall_dists |> \n  knitr::kable(digits = 2)\n```\n\n::: {.cell-output-display}\n\n\n|Distribution | var_mse| var_notmse| varbias| notvarbias| var_prec| notvar_prec| var_mean| not_var_mean|\n|:------------|-------:|----------:|-------:|----------:|--------:|-----------:|--------:|------------:|\n|Poisson      |    0.66|       0.44|   -0.02|      -0.12|     2.10|        2.33|     1.98|         1.88|\n|Binomial     |    0.64|       0.44|   -0.04|      -0.13|     2.15|        2.38|     1.96|         1.87|\n|Normal       |    0.49|       0.45|    0.00|      -0.10|     2.03|        2.25|     2.00|         1.90|\n|Exponential  |    1.43|       1.30|   -0.02|      -0.12|     0.70|        0.77|     1.98|         1.88|\n\n\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\npoisson_var = ggplot(results_p) +\n    geom_boxplot(aes(x=\"\", y = var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"Variance estimator\")\n\npoisson_notvar = ggplot(results_p) +\n    geom_boxplot(aes(x=\"\", y = not_var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"'Not-variance' estimator\")\n```\n:::\n\n\n \\normalsize\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\npoisson_var + poisson_notvar\n```\n\n::: {.cell-output-display}\n![Boxplots comparing the variances of the two estimators for variance, with data from the Poisson distribution](Lab11WriteUp_files/figure-pdf/fig-box-poisson-results-1.pdf){#fig-box-poisson-results fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\n\n\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbinom_var = ggplot(results_b) +\n    geom_boxplot(aes(x=\"\", y = var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"Variance estimator\")\n\nbinom_notvar = ggplot(results_b) +\n    geom_boxplot(aes(x=\"\", y = not_var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"'Not-variance' estimator\")\n```\n:::\n\n\n \\normalsize\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\nbinom_var + binom_notvar\n```\n\n::: {.cell-output-display}\n![Boxplots comparing the variances of the two estimators for variance, with data from the Binomial distribution](Lab11WriteUp_files/figure-pdf/fig-box-binom-results-1.pdf){#fig-box-binom-results fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nnorm_var = ggplot(results_n) +\n    geom_boxplot(aes(x=\"\", y = var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"Variance estimator\")\n\nnorm_notvar = ggplot(results_n) +\n    geom_boxplot(aes(x=\"\", y = not_var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"'Not-variance' estimator\")\n```\n:::\n\n\n \\normalsize\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\nnorm_var + norm_notvar\n```\n\n::: {.cell-output-display}\n![Boxplots comparing the variances of the two estimators for variance, with data from the Normal distribution](Lab11WriteUp_files/figure-pdf/fig-box-norm-results-1.pdf){#fig-box-norm-results fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nexp_var = ggplot(results_n) +\n    geom_boxplot(aes(x=\"\", y = var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"Variance estimator\")\n\nexp_notvar = ggplot(results_n) +\n    geom_boxplot(aes(x=\"\", y = not_var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"'Not-variance' estimator\")\n```\n:::\n\n\n \\normalsize\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\nexp_var + exp_notvar\n```\n\n::: {.cell-output-display}\n![Boxplots comparing the variances of the two estimators for variance, with data from the Exponential distribution](Lab11WriteUp_files/figure-pdf/fig-box-exp-results-1.pdf){#fig-box-exp-results fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\n\n## Simulation over various sample sizes\nConsider the effect of the sample size have on these results. Provide a graphical and numerical summary of what happens as $n$ increases -- say $n=20$, $n=50$, $n=100$, and $n=500$. Does the result in your initial simulation hold across samples? Or, do things change as the sample size changes? Ensure to set your randomization seed.\n \n**Solution**\n\nHere, I reuse my original code, and change n to 50.\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(7272)\nn = 50\nn.sims=1000\n# Poisson \nlambda = 2\nresults_p = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rpois(n, lambda)\n (results_p$var[i] = var(x) ) # variance\n (results_p$not_var[i] = (n-1)/n * var(x)) # the not variance - avg. squared dist from mean\n}\nmeans_p = tibble(var_mean = mean(results_p$var), not_var_mean = mean(results_p$not_var))\n\n# Binomial\nsize = 50\np = (5-sqrt(21))/10\nresults_b = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rbinom(n, size, p)\n (results_b$var[i] = var(x) ) # variance\n (results_b$not_var[i] = (n-1)/n * var(x)) # the not variance - avg. squared dist from mean\n}\nmeans_b = tibble(var_mean = mean(results_b$var), not_var_mean = mean(results_b$not_var))\n\n# Normal\nmean = 0\nsd = sqrt(2)\nresults_n = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rnorm(n, mean, sd)\n (results_n$var[i] = var(x) ) # variance\n (results_n$not_var[i] = (n-1)/n * var(x)) # the not variance - avg. squared dist from mean\n}\nmeans_n = tibble(var_mean = mean(results_n$var), not_var_mean = mean(results_n$not_var))\n\n# Exponential\nlambda = 1/sqrt(2)\nresults_e = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rexp(n, lambda)\n (results_e$var[i] = var(x) ) # variance\n (results_e$not_var[i] = (n-1)/n * var(x)) # the \"not\" variance - avg. squared dist from mean\n}\nmeans_e = tibble(var_mean = mean(results_e$var), not_var_mean = mean(results_e$not_var))\n# biases\nbias_b = tibble(varbias = mean(results_b$var) - 2, notvarbias=mean(results_b$not_var) - 2)\nbias_p = tibble(varbias = mean(results_p$var) - 2, notvarbias=mean(results_p$not_var) - 2)\nbias_n = tibble(varbias = mean(results_n$var) - 2, notvarbias=mean(results_n$not_var) - 2)\nbias_e = tibble(varbias = mean(results_e$var) - 2, notvarbias=mean(results_e$not_var) - 2)\n\n# precisions\nprecision_b = tibble(var_prec = 1/var(results_b$var), notvar_prec = 1/var(results_b$not_var))\nprecision_p = tibble(var_prec = 1/var(results_p$var), notvar_prec = 1/var(results_p$not_var))\nprecision_n = tibble(var_prec = 1/var(results_n$var), notvar_prec = 1/var(results_n$not_var))\nprecision_e = tibble(var_prec = 1/var(results_e$var), notvar_prec = 1/var(results_e$not_var))\n\n# MSE\nmse_b = tibble(var_mse = (bias_b$varbias)^2 + var(results_b$var),\nvar_notmse = bias_b$notvarbias)^2 + var(results_b$not_var)\n\nmse_p = tibble(var_mse = (bias_p$varbias)^2 + var(results_p$var),\nvar_notmse = bias_p$notvarbias)^2 + var(results_p$not_var)\n\nmse_n = tibble(var_mse = (bias_n$varbias)^2 + var(results_n$var),\nvar_notmse = (bias_n$notvarbias)^2 + var(results_n$not_var))\n\nmse_e = tibble(var_mse = (bias_e$varbias)^2 + var(results_e$var),\nvar_notmse = (bias_e$notvarbias)^2 + var(results_e$not_var))\n\n\n# combining all calculated values into a tibble for each distrib\ncombined_p = bind_cols(mse_p, bias_p, precision_p, means_p)\ncombined_b = bind_cols(mse_b, bias_b, precision_b, means_b)\ncombined_e = bind_cols(mse_e, bias_e, precision_e, means_e)\ncombined_n = bind_cols(mse_n, bias_n, precision_n, means_n)\n\nall_dists = bind_rows(combined_p, combined_b, combined_n, combined_e) |>\n  add_column(Distribution = c(\"Poisson\", \"Binomial\", \"Normal\", \"Exponential\"), .before = 1)\n```\n:::\n\n\n \\normalsize\n\n\n \\scriptsize\n\n\n::: {#tbl-dists-compared-n50 .cell tbl-cap='Different properties of the distributions, when n = 100' tbl-align='center'}\n\n```{.r .cell-code}\nall_dists |> \n  knitr::kable(digits = 2)\n```\n\n::: {.cell-output-display}\n\n\n|Distribution | var_mse| var_notmse| varbias| notvarbias| var_prec| notvar_prec| var_mean| not_var_mean|\n|:------------|-------:|----------:|-------:|----------:|--------:|-----------:|--------:|------------:|\n|Poisson      |    0.22|       0.19|   -0.03|      -0.07|     5.17|        5.39|     1.97|         1.93|\n|Binomial     |    0.22|       0.18|   -0.01|      -0.05|     5.31|        5.53|     1.99|         1.95|\n|Normal       |    0.17|       0.16|   -0.01|      -0.05|     5.98|        6.23|     1.99|         1.95|\n|Exponential  |    0.70|       0.67|   -0.02|      -0.06|     1.43|        1.49|     1.98|         1.94|\n\n\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\npoisson_var = ggplot(results_p) +\n    geom_boxplot(aes(x=\"\", y = var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"Variance estimator\")\n\npoisson_notvar = ggplot(results_p) +\n    geom_boxplot(aes(x=\"\", y = not_var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"'Not-variance' estimator\")\n```\n:::\n\n\n \\normalsize\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\npoisson_var + poisson_notvar\n```\n\n::: {.cell-output-display}\n![Boxplots comparing the variances of the two estimators for variance, with data from the Poisson distribution, when n = 50](Lab11WriteUp_files/figure-pdf/fig-box-poisson-results-n50-1.pdf){#fig-box-poisson-results-n50 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbinom_var = ggplot(results_b) +\n    geom_boxplot(aes(x=\"\", y = var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"Variance estimator\")\n\nbinom_notvar = ggplot(results_b) +\n    geom_boxplot(aes(x=\"\", y = not_var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"'Not-variance' estimator\")\n```\n:::\n\n\n \\normalsize\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\nbinom_var + binom_notvar\n```\n\n::: {.cell-output-display}\n![Boxplots comparing the variances of the two estimators for variance, with data from the Binomial distribution, when n = 50](Lab11WriteUp_files/figure-pdf/fig-box-binom-results-n50-1.pdf){#fig-box-binom-results-n50 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nnorm_var = ggplot(results_n) +\n    geom_boxplot(aes(x=\"\", y = var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"Variance estimator\")\n\nnorm_notvar = ggplot(results_n) +\n    geom_boxplot(aes(x=\"\", y = not_var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"'Not-variance' estimator\")\n```\n:::\n\n\n \\normalsize\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\nnorm_var + norm_notvar\n```\n\n::: {.cell-output-display}\n![Boxplots comparing the variances of the two estimators for variance, with data from the Normal distribution, where n = 50](Lab11WriteUp_files/figure-pdf/fig-box-norm-results-n50-1.pdf){#fig-box-norm-results-n50 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nexp_var = ggplot(results_n) +\n    geom_boxplot(aes(x=\"\", y = var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"Variance estimator\")\n\nexp_notvar = ggplot(results_n) +\n    geom_boxplot(aes(x=\"\", y = not_var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"'Not-variance' estimator\")\n```\n:::\n\n\n \\normalsize\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\nexp_var + exp_notvar\n```\n\n::: {.cell-output-display}\n![Boxplots comparing the variances of the two estimators for variance, with data from the Exponential distribution where n = 50](Lab11WriteUp_files/figure-pdf/fig-box-exp-results-n50-1.pdf){#fig-box-exp-results-n50 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\nHere, I do the same thing, but change n to 100.\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(7272)\nn = 100\nn.sims=1000\n# Poisson \nlambda = 2\nresults_p = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rpois(n, lambda)\n (results_p$var[i] = var(x) ) # variance\n (results_p$not_var[i] = (n-1)/n * var(x)) # the not variance - avg. squared dist from mean\n}\nmeans_p = tibble(var_mean = mean(results_p$var), not_var_mean = mean(results_p$not_var))\n\n# Binomial\nsize = 50\np = (5-sqrt(21))/10\nresults_b = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rbinom(n, size, p)\n (results_b$var[i] = var(x) ) # variance\n (results_b$not_var[i] = (n-1)/n * var(x)) # the not variance - avg. squared dist from mean\n}\nmeans_b = tibble(var_mean = mean(results_b$var), not_var_mean = mean(results_b$not_var))\n\n# Normal\nmean = 0\nsd = sqrt(2)\nresults_n = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rnorm(n, mean, sd)\n (results_n$var[i] = var(x) ) # variance\n (results_n$not_var[i] = (n-1)/n * var(x)) # the not variance - avg. squared dist from mean\n}\nmeans_n = tibble(var_mean = mean(results_n$var), not_var_mean = mean(results_n$not_var))\n\n# Exponential\nlambda = 1/sqrt(2)\nresults_e = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rexp(n, lambda)\n (results_e$var[i] = var(x) ) # variance\n (results_e$not_var[i] = (n-1)/n * var(x)) # the \"not\" variance - avg. squared dist from mean\n}\nmeans_e = tibble(var_mean = mean(results_e$var), not_var_mean = mean(results_e$not_var))\n# biases\nbias_b = tibble(varbias = mean(results_b$var) - 2, notvarbias=mean(results_b$not_var) - 2)\nbias_p = tibble(varbias = mean(results_p$var) - 2, notvarbias=mean(results_p$not_var) - 2)\nbias_n = tibble(varbias = mean(results_n$var) - 2, notvarbias=mean(results_n$not_var) - 2)\nbias_e = tibble(varbias = mean(results_e$var) - 2, notvarbias=mean(results_e$not_var) - 2)\n\n# precisions\nprecision_b = tibble(var_prec = 1/var(results_b$var), notvar_prec = 1/var(results_b$not_var))\nprecision_p = tibble(var_prec = 1/var(results_p$var), notvar_prec = 1/var(results_p$not_var))\nprecision_n = tibble(var_prec = 1/var(results_n$var), notvar_prec = 1/var(results_n$not_var))\nprecision_e = tibble(var_prec = 1/var(results_e$var), notvar_prec = 1/var(results_e$not_var))\n\n# MSE\nmse_b = tibble(var_mse = (bias_b$varbias)^2 + var(results_b$var),\nvar_notmse = bias_b$notvarbias)^2 + var(results_b$not_var)\n\nmse_p = tibble(var_mse = (bias_p$varbias)^2 + var(results_p$var),\nvar_notmse = bias_p$notvarbias)^2 + var(results_p$not_var)\n\nmse_n = tibble(var_mse = (bias_n$varbias)^2 + var(results_n$var),\nvar_notmse = (bias_n$notvarbias)^2 + var(results_n$not_var))\n\nmse_e = tibble(var_mse = (bias_e$varbias)^2 + var(results_e$var),\nvar_notmse = (bias_e$notvarbias)^2 + var(results_e$not_var))\n\n\n# combining all calculated values into a tibble for each distrib\ncombined_p = bind_cols(mse_p, bias_p, precision_p, means_p)\ncombined_b = bind_cols(mse_b, bias_b, precision_b, means_b)\ncombined_e = bind_cols(mse_e, bias_e, precision_e, means_e)\ncombined_n = bind_cols(mse_n, bias_n, precision_n, means_n)\n\nall_dists = bind_rows(combined_p, combined_b, combined_n, combined_e) |>\n  add_column(Distribution = c(\"Poisson\", \"Binomial\", \"Normal\", \"Exponential\"), .before = 1)\n```\n:::\n\n\n \\normalsize\n\n\n \\scriptsize\n\n\n::: {#tbl-dists-compared-n100 .cell tbl-cap='Different properties of the distributions, when n = 100' tbl-align='center'}\n\n```{.r .cell-code}\nall_dists |> \n  knitr::kable(digits = 2)\n```\n\n::: {.cell-output-display}\n\n\n|Distribution | var_mse| var_notmse| varbias| notvarbias| var_prec| notvar_prec| var_mean| not_var_mean|\n|:------------|-------:|----------:|-------:|----------:|--------:|-----------:|--------:|------------:|\n|Poisson      |    0.11|       0.10|   -0.02|      -0.04|    10.07|       10.27|     1.98|         1.96|\n|Binomial     |    0.12|       0.11|    0.00|      -0.02|     9.30|        9.49|     2.00|         1.98|\n|Normal       |    0.09|       0.09|    0.01|      -0.01|    11.37|       11.60|     2.01|         1.99|\n|Exponential  |    0.30|       0.29|    0.02|       0.00|     3.33|        3.40|     2.02|         2.00|\n\n\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\npoisson_var = ggplot(results_p) +\n    geom_boxplot(aes(x=\"\", y = var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"Variance estimator\")\n\npoisson_notvar = ggplot(results_p) +\n    geom_boxplot(aes(x=\"\", y = not_var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"'Not-variance' estimator\")\n```\n:::\n\n\n \\normalsize\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\npoisson_var + poisson_notvar\n```\n\n::: {.cell-output-display}\n![Boxplots comparing the variances of the two estimators for variance, with data from the Poisson distribution, when n = 100](Lab11WriteUp_files/figure-pdf/fig-box-poisson-results-n100-1.pdf){#fig-box-poisson-results-n100 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbinom_var = ggplot(results_b) +\n    geom_boxplot(aes(x=\"\", y = var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"Variance estimator\")\n\nbinom_notvar = ggplot(results_b) +\n    geom_boxplot(aes(x=\"\", y = not_var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"'Not-variance' estimator\")\n```\n:::\n\n\n \\normalsize\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\nbinom_var + binom_notvar\n```\n\n::: {.cell-output-display}\n![Boxplots comparing the variances of the two estimators for variance, with data from the Binomial distribution, when n = 100](Lab11WriteUp_files/figure-pdf/fig-box-binom-results-n100-1.pdf){#fig-box-binom-results-n100 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nnorm_var = ggplot(results_n) +\n    geom_boxplot(aes(x=\"\", y = var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"Variance estimator\")\n\nnorm_notvar = ggplot(results_n) +\n    geom_boxplot(aes(x=\"\", y = not_var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"'Not-variance' estimator\")\n```\n:::\n\n\n \\normalsize\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\nnorm_var + norm_notvar\n```\n\n::: {.cell-output-display}\n![Boxplots comparing the variances of the two estimators for variance, with data from the Normal distribution, where n = 100](Lab11WriteUp_files/figure-pdf/fig-box-norm-results-n100-1.pdf){#fig-box-norm-results-n100 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nexp_var = ggplot(results_n) +\n    geom_boxplot(aes(x=\"\", y = var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"Variance estimator\")\n\nexp_notvar = ggplot(results_n) +\n    geom_boxplot(aes(x=\"\", y = not_var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"'Not-variance' estimator\")\n```\n:::\n\n\n \\normalsize\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\nexp_var + exp_notvar\n```\n\n::: {.cell-output-display}\n![Boxplots comparing the variances of the two estimators for variance, with data from the Exponential distribution where n = 100](Lab11WriteUp_files/figure-pdf/fig-box-exp-results-n100-1.pdf){#fig-box-exp-results-n100 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\nFinally, I do the same thing again but with n = 500.\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(7272)\nn = 500\nn.sims=1000\n# Poisson \nlambda = 2\nresults_p = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rpois(n, lambda)\n (results_p$var[i] = var(x) ) # variance\n (results_p$not_var[i] = (n-1)/n * var(x)) # the not variance - avg. squared dist from mean\n}\nmeans_p = tibble(var_mean = mean(results_p$var), not_var_mean = mean(results_p$not_var))\n\n# Binomial\nsize = 50\np = (5-sqrt(21))/10\nresults_b = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rbinom(n, size, p)\n (results_b$var[i] = var(x) ) # variance\n (results_b$not_var[i] = (n-1)/n * var(x)) # the not variance - avg. squared dist from mean\n}\nmeans_b = tibble(var_mean = mean(results_b$var), not_var_mean = mean(results_b$not_var))\n\n# Normal\nmean = 0\nsd = sqrt(2)\nresults_n = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rnorm(n, mean, sd)\n (results_n$var[i] = var(x) ) # variance\n (results_n$not_var[i] = (n-1)/n * var(x)) # the not variance - avg. squared dist from mean\n}\nmeans_n = tibble(var_mean = mean(results_n$var), not_var_mean = mean(results_n$not_var))\n\n# Exponential\nlambda = 1/sqrt(2)\nresults_e = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rexp(n, lambda)\n (results_e$var[i] = var(x) ) # variance\n (results_e$not_var[i] = (n-1)/n * var(x)) # the \"not\" variance - avg. squared dist from mean\n}\nmeans_e = tibble(var_mean = mean(results_e$var), not_var_mean = mean(results_e$not_var))\n# biases\nbias_b = tibble(varbias = mean(results_b$var) - 2, notvarbias=mean(results_b$not_var) - 2)\nbias_p = tibble(varbias = mean(results_p$var) - 2, notvarbias=mean(results_p$not_var) - 2)\nbias_n = tibble(varbias = mean(results_n$var) - 2, notvarbias=mean(results_n$not_var) - 2)\nbias_e = tibble(varbias = mean(results_e$var) - 2, notvarbias=mean(results_e$not_var) - 2)\n\n# precisions\nprecision_b = tibble(var_prec = 1/var(results_b$var), notvar_prec = 1/var(results_b$not_var))\nprecision_p = tibble(var_prec = 1/var(results_p$var), notvar_prec = 1/var(results_p$not_var))\nprecision_n = tibble(var_prec = 1/var(results_n$var), notvar_prec = 1/var(results_n$not_var))\nprecision_e = tibble(var_prec = 1/var(results_e$var), notvar_prec = 1/var(results_e$not_var))\n\n# MSE\nmse_b = tibble(var_mse = (bias_b$varbias)^2 + var(results_b$var),\nvar_notmse = bias_b$notvarbias)^2 + var(results_b$not_var)\n\nmse_p = tibble(var_mse = (bias_p$varbias)^2 + var(results_p$var),\nvar_notmse = bias_p$notvarbias)^2 + var(results_p$not_var)\n\nmse_n = tibble(var_mse = (bias_n$varbias)^2 + var(results_n$var),\nvar_notmse = (bias_n$notvarbias)^2 + var(results_n$not_var))\n\nmse_e = tibble(var_mse = (bias_e$varbias)^2 + var(results_e$var),\nvar_notmse = (bias_e$notvarbias)^2 + var(results_e$not_var))\n\n\n# combining all calculated values into a tibble for each distrib\ncombined_p = bind_cols(mse_p, bias_p, precision_p, means_p)\ncombined_b = bind_cols(mse_b, bias_b, precision_b, means_b)\ncombined_e = bind_cols(mse_e, bias_e, precision_e, means_e)\ncombined_n = bind_cols(mse_n, bias_n, precision_n, means_n)\n\nall_dists = bind_rows(combined_p, combined_b, combined_n, combined_e) |>\n  add_column(Distribution = c(\"Poisson\", \"Binomial\", \"Normal\", \"Exponential\"), .before = 1)\n```\n:::\n\n\n \\normalsize\n\n\n \\scriptsize\n\n\n::: {#tbl-dists-compared-n500 .cell tbl-cap='Different properties of the distributions' tbl-align='center'}\n\n```{.r .cell-code}\nall_dists |> \n  knitr::kable(digits = 2)\n```\n\n::: {.cell-output-display}\n\n\n|Distribution | var_mse| var_notmse| varbias| notvarbias| var_prec| notvar_prec| var_mean| not_var_mean|\n|:------------|-------:|----------:|-------:|----------:|--------:|-----------:|--------:|------------:|\n|Poisson      |    0.02|       0.02|       0|      -0.01|    50.32|       50.52|        2|         1.99|\n|Binomial     |    0.02|       0.02|       0|      -0.01|    55.41|       55.63|        2|         1.99|\n|Normal       |    0.02|       0.02|       0|       0.00|    58.58|       58.81|        2|         2.00|\n|Exponential  |    0.06|       0.06|       0|       0.00|    15.71|       15.77|        2|         2.00|\n\n\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\npoisson_var = ggplot(results_p) +\n    geom_boxplot(aes(x=\"\", y = var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"Variance estimator\")\n\npoisson_notvar = ggplot(results_p) +\n    geom_boxplot(aes(x=\"\", y = not_var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"'Not-variance' estimator\")\n```\n:::\n\n\n \\normalsize\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\npoisson_var + poisson_notvar\n```\n\n::: {.cell-output-display}\n![Boxplots comparing the variances of the two estimators for variance, with data from the Poisson distribution, when n = 500](Lab11WriteUp_files/figure-pdf/fig-box-poisson-results-n500-1.pdf){#fig-box-poisson-results-n500 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbinom_var = ggplot(results_b) +\n    geom_boxplot(aes(x=\"\", y = var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"Variance estimator\")\n\nbinom_notvar = ggplot(results_b) +\n    geom_boxplot(aes(x=\"\", y = not_var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"'Not-variance' estimator\")\n```\n:::\n\n\n \\normalsize\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\nbinom_var + binom_notvar\n```\n\n::: {.cell-output-display}\n![Boxplots comparing the variances of the two estimators for variance, with data from the Binomial distribution, when n = 500](Lab11WriteUp_files/figure-pdf/fig-box-binom-results-n500-1.pdf){#fig-box-binom-results-n500 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nnorm_var = ggplot(results_n) +\n    geom_boxplot(aes(x=\"\", y = var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"Variance estimator\")\n\nnorm_notvar = ggplot(results_n) +\n    geom_boxplot(aes(x=\"\", y = not_var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"'Not-variance' estimator\")\n```\n:::\n\n\n \\normalsize\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\nnorm_var + norm_notvar\n```\n\n::: {.cell-output-display}\n![Boxplots comparing the variances of the two estimators for variance, with data from the Normal distribution, where n = 500](Lab11WriteUp_files/figure-pdf/fig-box-norm-results-n500-1.pdf){#fig-box-norm-results-n500 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nexp_var = ggplot(results_n) +\n    geom_boxplot(aes(x=\"\", y = var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"Variance estimator\")\n\nexp_notvar = ggplot(results_n) +\n    geom_boxplot(aes(x=\"\", y = not_var), width=.1) +\n    geom_hline(yintercept = 2, color = \"red\") +\n    theme_bw() + \n    labs(x = \"\", y=\"Variance\", title = \"'Not-variance' estimator\")\n```\n:::\n\n\n \\normalsize\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\nexp_var + exp_notvar\n```\n\n::: {.cell-output-display}\n![Boxplots comparing the variances of the two estimators for variance, with data from the Exponential distribution where n = 500](Lab11WriteUp_files/figure-pdf/fig-box-exp-results-n500-1.pdf){#fig-box-exp-results-n500 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\nLooking at the different tables produced for varying values of n, we can see that the bias and mse approaches 0 as n increases. We can also see that the mean appproaches 2, and that the precision increases as n increases. The boxplots show the same trend, which can be seen by the median approaching the true mean of 2, suggesting that as the size of the data increases, outliers have a smaller effect on the data. However, the variance is still more accurate than the \"not-variance\", even at larger sample sizes, although the difference is not as large as it was at small sizes. \n\n## Simulation\nYou have written code to draw a random sample of various sizes from each distribution, and to calculate and store the calculated estimates. Rework your code to produce a density plot of the $s_{n-1}^2$ and $s_{n}^2$. Do you have a guess about their distribution? Ensure to set your randomization seed. \n\n**Solution**\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 20\n# Poisson \nlambda = 2\nresults_p = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rpois(n, lambda)\n (results_p$var[i] = var(x) ) # variance\n (results_p$not_var[i] = (n-1)/n * var(x)) # the not variance - avg. squared dist from mean\n}\n```\n:::\n\n\n \\normalsize\n\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\n#poisson\nset.seed(7272)\npvar_dplot=ggplot(data=results_p) +\n  stat_density(aes(x=var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n-1)^2\")\npnotvar_dplot=ggplot(data=results_p) +\n  stat_density(aes(x=not_var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n))^2\")\n\npvar_dplot + pnotvar_dplot\n```\n\n::: {.cell-output-display}\n![Density plots of the (s_n)^2 and the (s_(n-1))^2 of the Poisson distribution when n = 20](Lab11WriteUp_files/figure-pdf/fig-poisson-comparison-n20-1.pdf){#fig-poisson-comparison-n20 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 50\n# Poisson \nlambda = 2\nresults_p = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rpois(n, lambda)\n (results_p$var[i] = var(x) ) # variance\n (results_p$not_var[i] = (n-1)/n * var(x)) # the not variance - avg. squared dist from mean\n}\n```\n:::\n\n\n \\normalsize\n\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\n#poisson\nset.seed(7272)\npvar_dplot=ggplot(data=results_p) +\n  stat_density(aes(x=var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n-1))^2\")\npnotvar_dplot=ggplot(data=results_p) +\n  stat_density(aes(x=not_var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n))^2\")\n\npvar_dplot + pnotvar_dplot\n```\n\n::: {.cell-output-display}\n![Density plots of the (s_n)^2 and the (s_(n-1))^2 of the Poisson distribution when n = 50](Lab11WriteUp_files/figure-pdf/fig-poisson-comparison-n50-1.pdf){#fig-poisson-comparison-n50 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 100\n# Poisson \nlambda = 2\nresults_p = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rpois(n, lambda)\n (results_p$var[i] = var(x) ) # variance\n (results_p$not_var[i] = (n-1)/n * var(x)) # the not variance - avg. squared dist from mean\n}\n```\n:::\n\n\n \\normalsize\n\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\n#poisson\nset.seed(7272)\npvar_dplot=ggplot(data=results_p) +\n  stat_density(aes(x=var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n-1))^2\")\npnotvar_dplot=ggplot(data=results_p) +\n  stat_density(aes(x=not_var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n))^2\")\n\npvar_dplot + pnotvar_dplot\n```\n\n::: {.cell-output-display}\n![Density plots of the (s_n)^2 and the (s_(n-1))^2 of the Poisson distribution when n = 100](Lab11WriteUp_files/figure-pdf/fig-poisson-comparison-n100-1.pdf){#fig-poisson-comparison-n100 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 500\n# Poisson \nlambda = 2\nresults_p = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rpois(n, lambda)\n (results_p$var[i] = var(x) ) # variance\n (results_p$not_var[i] = (n-1)/n * var(x)) # the not variance - avg. squared dist from mean\n}\n```\n:::\n\n\n \\normalsize\n\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\n#poisson\nset.seed(7272)\npvar_dplot=ggplot(data=results_p) +\n  stat_density(aes(x=var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n-1)^2\")\npnotvar_dplot=ggplot(data=results_p) +\n  stat_density(aes(x=not_var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n))^2\")\n\npvar_dplot + pnotvar_dplot\n```\n\n::: {.cell-output-display}\n![Density plots of the (s_n)^2 and the (s_(n-1))^2 of the Poisson distribution when n = 500](Lab11WriteUp_files/figure-pdf/fig-poisson-comparison-n500-1.pdf){#fig-poisson-comparison-n500 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 20\n# binomial \nlambda = 2\nresults_b = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rpois(n, lambda)\n (results_b$var[i] = var(x) ) # variance\n (results_b$not_var[i] = (n-1)/n * var(x)) # the not variance - avg. squared dist from mean\n}\n```\n:::\n\n\n \\normalsize\n\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\nset.seed(7272)\nbvar_dplot=ggplot(data=results_b) +\n  stat_density(aes(x=var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n-1))^2\")\nbnotvar_dplot=ggplot(data=results_b) +\n  stat_density(aes(x=not_var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n))^2\")\n\nbvar_dplot + bnotvar_dplot\n```\n\n::: {.cell-output-display}\n![Density plots of the (s_n)^2 and the (s_(n-1))^2 of the Binomial distribution when n = 20](Lab11WriteUp_files/figure-pdf/fig-binomial-comparison-n20-1.pdf){#fig-binomial-comparison-n20 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 50\n# binomial \nlambda = 2\nresults_b = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rpois(n, lambda)\n (results_b$var[i] = var(x) ) # variance\n (results_b$not_var[i] = (n-1)/n * var(x)) # the not variance - avg. squared dist from mean\n}\n```\n:::\n\n\n \\normalsize\n\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\nset.seed(7272)\nbvar_dplot=ggplot(data=results_b) +\n  stat_density(aes(x=var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n-1))^2\")\nbnotvar_dplot=ggplot(data=results_b) +\n  stat_density(aes(x=not_var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n))^2\")\n\nbvar_dplot + bnotvar_dplot\n```\n\n::: {.cell-output-display}\n![Density plots of the (s_n)^2 and the (s_(n-1))^2 of the Binomial distribution when n = 50](Lab11WriteUp_files/figure-pdf/fig-binomial-comparison-n50-1.pdf){#fig-binomial-comparison-n50 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 100\n# binomial \nlambda = 2\nresults_b = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rpois(n, lambda)\n (results_b$var[i] = var(x) ) # variance\n (results_b$not_var[i] = (n-1)/n * var(x)) # the not variance - avg. squared dist from mean\n}\n```\n:::\n\n\n \\normalsize\n\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\n#poisson\nset.seed(7272)\nbvar_dplot=ggplot(data=results_b) +\n  stat_density(aes(x=var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n-1))^2\")\nbnotvar_dplot=ggplot(data=results_b) +\n  stat_density(aes(x=not_var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n))^2\")\n\nbvar_dplot + bnotvar_dplot\n```\n\n::: {.cell-output-display}\n![Density plots of the (s_n)^2 and the (s_(n-1))^2 of the Binomial distribution when n = 100](Lab11WriteUp_files/figure-pdf/fig-binomial-comparison-n100-1.pdf){#fig-binomial-comparison-n100 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 500\n# binomial \nlambda = 2\nresults_b = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rpois(n, lambda)\n (results_b$var[i] = var(x) ) # variance\n (results_b$not_var[i] = (n-1)/n * var(x)) # the not variance - avg. squared dist from mean\n}\n```\n:::\n\n\n \\normalsize\n\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\n#poisson\nset.seed(7272)\nbvar_dplot=ggplot(data=results_b) +\n  stat_density(aes(x=var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n-1))^2\")\nbnotvar_dplot=ggplot(data=results_b) +\n  stat_density(aes(x=not_var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n))^2\")\n\nbvar_dplot + bnotvar_dplot\n```\n\n::: {.cell-output-display}\n![Density plots of the (s_n)^2 and the (s_(n-1))^2 of the Binomial distribution when n = 500](Lab11WriteUp_files/figure-pdf/fig-binomial-comparison-n500-1.pdf){#fig-binomial-comparison-n500 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 20\n# exponential \nlambda = 2\nresults_e = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rpois(n, lambda)\n (results_e$var[i] = var(x) ) # variance\n (results_e$not_var[i] = (n-1)/n * var(x)) # the not variance - avg. squared dist from mean\n}\n```\n:::\n\n\n \\normalsize\n\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\n#poisson\nset.seed(7272)\nevar_dplot=ggplot(data=results_e) +\n  stat_density(aes(x=var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n-1))^2\")\nenotvar_dplot=ggplot(data=results_e) +\n  stat_density(aes(x=not_var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n))^2\")\n\nevar_dplot + enotvar_dplot\n```\n\n::: {.cell-output-display}\n![Density plots of the (s_n)^2 and the (s_(n-1))^2 of the Exponential distribution when n = 20](Lab11WriteUp_files/figure-pdf/fig-exponential-comparison-n20-1.pdf){#fig-exponential-comparison-n20 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 50\n# exponential \nlambda = 2\nresults_e = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rpois(n, lambda)\n (results_e$var[i] = var(x) ) # variance\n (results_e$not_var[i] = (n-1)/n * var(x)) # the not variance - avg. squared dist from mean\n}\n```\n:::\n\n\n \\normalsize\n\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\n#poisson\nset.seed(7272)\nevar_dplot=ggplot(data=results_e) +\n  stat_density(aes(x=var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n-1))^2\")\nenotvar_dplot=ggplot(data=results_e) +\n  stat_density(aes(x=not_var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n))^2\")\n\nevar_dplot + enotvar_dplot\n```\n\n::: {.cell-output-display}\n![Density plots of the (s_n)^2 and the (s_(n-1))^2 of the Exponential distribution when n = 50](Lab11WriteUp_files/figure-pdf/fig-exponential-comparison-n50-1.pdf){#fig-exponential-comparison-n50 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 100\n# exponential \nlambda = 2\nresults_e = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rpois(n, lambda)\n (results_e$var[i] = var(x) ) # variance\n (results_e$not_var[i] = (n-1)/n * var(x)) # the not variance - avg. squared dist from mean\n}\n```\n:::\n\n\n \\normalsize\n\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\n#poisson\nset.seed(7272)\nevar_dplot=ggplot(data=results_e) +\n  stat_density(aes(x=var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n-1))^2\")\nenotvar_dplot=ggplot(data=results_e) +\n  stat_density(aes(x=not_var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n))^2\")\n\nevar_dplot + enotvar_dplot\n```\n\n::: {.cell-output-display}\n![Density plots of the (s_n)^2 and the (s_(n-1))^2 of the Exponential distribution when n = 100](Lab11WriteUp_files/figure-pdf/fig-exponential-comparison-n100-1.pdf){#fig-exponential-comparison-n100 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 500\n# exponential \nlambda = 2\nresults_e = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rpois(n, lambda)\n (results_e$var[i] = var(x) ) # variance\n (results_e$not_var[i] = (n-1)/n * var(x)) # the not variance - avg. squared dist from mean\n}\n```\n:::\n\n\n \\normalsize\n\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\n#poisson\nset.seed(7272)\nevar_dplot=ggplot(data=results_e) +\n  stat_density(aes(x=var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n-1))^2\")\nenotvar_dplot=ggplot(data=results_e) +\n  stat_density(aes(x=not_var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n))^2\")\n\nevar_dplot + enotvar_dplot\n```\n\n::: {.cell-output-display}\n![Density plots of the (s_n)^2 and the (s_(n-1))^2 of the Exponential distribution when n = 500](Lab11WriteUp_files/figure-pdf/fig-exponential-comparison-n500-1.pdf){#fig-exponential-comparison-n500 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 20\n# normal \nlambda = 2\nresults_n = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rpois(n, lambda)\n (results_n$var[i] = var(x) ) # variance\n (results_n$not_var[i] = (n-1)/n * var(x)) # the not variance - avg. squared dist from mean\n}\n```\n:::\n\n\n \\normalsize\n\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\nset.seed(7272)\nnvar_dplot=ggplot(data=results_n) +\n  stat_density(aes(x=var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n-1))^2\")\nnnotvar_dplot=ggplot(data=results_n) +\n  stat_density(aes(x=not_var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n))^2\")\n\nnvar_dplot + nnotvar_dplot\n```\n\n::: {.cell-output-display}\n![Density plots of the (s_n)^2 and the (s_(n-1))^2 of the Normal distribution when n = 20](Lab11WriteUp_files/figure-pdf/fig-normal-comparison-n20-1.pdf){#fig-normal-comparison-n20 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 50\n# normal \nlambda = 2\nresults_n = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rpois(n, lambda)\n (results_n$var[i] = var(x) ) # variance\n (results_n$not_var[i] = (n-1)/n * var(x)) # the not variance - avg. squared dist from mean\n}\n```\n:::\n\n\n \\normalsize\n\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\nset.seed(7272)\nnvar_dplot=ggplot(data=results_n) +\n  stat_density(aes(x=var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n-1))^2\")\nnnotvar_dplot=ggplot(data=results_n) +\n  stat_density(aes(x=not_var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n))^2\")\n\nnvar_dplot + nnotvar_dplot\n```\n\n::: {.cell-output-display}\n![Density plots of the (s_n)^2 and the (s_(n-1))^2 of the Normal distribution when n = 50](Lab11WriteUp_files/figure-pdf/fig-normal-comparison-n50-1.pdf){#fig-normal-comparison-n50 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 100\n# normal \nlambda = 2\nresults_n = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rpois(n, lambda)\n (results_n$var[i] = var(x) ) # variance\n (results_n$not_var[i] = (n-1)/n * var(x)) # the not variance - avg. squared dist from mean\n}\n```\n:::\n\n\n \\normalsize\n\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\nset.seed(7272)\nnvar_dplot=ggplot(data=results_n) +\n  stat_density(aes(x=var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n-1))^2\")\nnnotvar_dplot=ggplot(data=results_n) +\n  stat_density(aes(x=not_var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n))^2\")\n\nnvar_dplot + nnotvar_dplot\n```\n\n::: {.cell-output-display}\n![Density plots of the (s_n)^2 and the (s_(n-1))^2 of the Normal distribution when n = 100](Lab11WriteUp_files/figure-pdf/fig-normal-comparison-n100-1.pdf){#fig-normal-comparison-n100 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 500\n# normal \nlambda = 2\nresults_n = tibble(var=numeric(n.sims), not_var=numeric(n.sims))\nfor(i in 1:n.sims){\n  x = rpois(n, lambda)\n (results_n$var[i] = var(x) ) # variance\n (results_n$not_var[i] = (n-1)/n * var(x)) # the not variance - avg. squared dist from mean\n}\n```\n:::\n\n\n \\normalsize\n\n\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\nset.seed(7272)\nnvar_dplot=ggplot(data=results_n) +\n  stat_density(aes(x=var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n-1))^2\")\nnnotvar_dplot=ggplot(data=results_n) +\n  stat_density(aes(x=not_var), color=\"chartreuse2\", geom=\"line\", linewidth=1) +\n  xlab(\"(S_(n))^2\")\n\nnvar_dplot + nnotvar_dplot\n```\n\n::: {.cell-output-display}\n![Density plots of the (s_n)^2 and the (s_(n-1))^2 of the Normal distribution when n = 500](Lab11WriteUp_files/figure-pdf/fig-normal-comparison-n500-1.pdf){#fig-normal-comparison-n500 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\nI think that the distribution of $s_{n-1}^2$ and $s_{n}^2$ are approximately normal with large enough n.\n\n## Bootstrap\nNow, take *one* sample from each distribution. Instead of drawing a random sample at each iteration, sample *with replacement* from the one sample. Produce a density plot of the $s_{n-1}^2$ and $s_{n}^2$. Do we get roughly the same information as we do in the full simulation? Try this over several seeds -- what do you notice? Ensure to set your randomization seed.\n\n**Solution**\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(7272)\nn = 20  \nb = 1000  \noriginal_sample = rpois(n, lambda = lambda)\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\nbootstrap_p20 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrapp20=ggplot(data = bootstrap_p20) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(7272)\nn = 50  \nb = 1000  \noriginal_sample = rpois(n, lambda = lambda)\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\nbootstrap_p50 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrapp50=ggplot(data = bootstrap_p50) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(7272)\nn = 100  \nb = 1000  \noriginal_sample = rpois(n, lambda = lambda)\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\nbootstrap_p100 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrapp100=ggplot(data = bootstrap_p100) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(7272)\nn = 500  \nb = 1000  \noriginal_sample = rpois(n, lambda = lambda)\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\nbootstrap_p500 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrapp500= ggplot(data = bootstrap_p500) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\nbootstrapp20 + bootstrapp50 \nbootstrapp100 + bootstrapp500\n```\n\n::: {.cell-output-display}\n![Density plots of the variance and the variance *(n-1)/n of the Poisson distribution, for values of n 20, 50, 100, and 500 in ascending order.](Lab11WriteUp_files/figure-pdf/fig-density-pois-s7272-1.pdf){#fig-density-pois-s7272-1 fig-align='center' fig-pos='H'}\n:::\n\n::: {.cell-output-display}\n![Density plots of the variance and the variance *(n-1)/n of the Poisson distribution, for values of n 20, 50, 100, and 500 in ascending order.](Lab11WriteUp_files/figure-pdf/fig-density-pois-s7272-2.pdf){#fig-density-pois-s7272-2 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\nThrough these plots, we can see that as n increases, the distributions of the variance and the variance *(n-1)/n become increasingly similar, and at $n=500$ they are almost indistinguishable. This is because as n increases, the quantity $(n-1)/n$ approaches one. Interestingly, the variance appears to be the same for these values of n, just the $(s_n)^2$ changes.\n\nNow, we do the same for a different seed. \n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(492)\nn = 20  \nb = 1000  \noriginal_sample = rpois(n, lambda = lambda)\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\nbootstrap_p20 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrapp20=ggplot(data = bootstrap_p20) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(492)\nn = 50  \nb = 1000  \noriginal_sample = rpois(n, lambda = lambda)\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\nbootstrap_p50 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrapp50=ggplot(data = bootstrap_p50) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(492)\nn = 100  \nb = 1000  \noriginal_sample = rpois(n, lambda = lambda)\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\nbootstrap_p100 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrapp100=ggplot(data = bootstrap_p100) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(492)\nn = 500  \nb = 1000  \noriginal_sample = rpois(n, lambda = lambda)\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\nbootstrap_p500 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrapp500= ggplot(data = bootstrap_p500) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\nbootstrapp20 + bootstrapp50 \nbootstrapp100 + bootstrapp500\n```\n\n::: {.cell-output-display}\n![Density plots of the variance and the variance *(n-1)/n of the Poisson distribution, for values of n 20, 50, 100, and 500 in ascending order.](Lab11WriteUp_files/figure-pdf/fig-density-pois-s492-1.pdf){#fig-density-pois-s492-1 fig-align='center' fig-pos='H'}\n:::\n\n::: {.cell-output-display}\n![Density plots of the variance and the variance *(n-1)/n of the Poisson distribution, for values of n 20, 50, 100, and 500 in ascending order.](Lab11WriteUp_files/figure-pdf/fig-density-pois-s492-2.pdf){#fig-density-pois-s492-2 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\nWith this new seed, we can see that the variance and $(s_n)^2$ follow a similar pattern as previously, where the variance does not change with increasing n but $(s_n)^2$ does, until they become nearly indistinguishable at $n = 500$. Notably, the graphs have a different shape compared to the last seed, showing how sampling with replacement in the bootstrap changes the observed variance. \n\nNow we repeat this process for the remaining distributions, starting with the Binomial distribution.\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(7272)\nn = 20   \noriginal_sample = rbinom(n = n, size = 50, prob = (5-sqrt(21))/2)\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\n\nbootstrap_b20 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrapb20= ggplot(data = bootstrap_b20) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 50 \noriginal_sample = rbinom(n = n, size = 50, prob = (5-sqrt(21))/2)\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\nbootstrap_b50 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrapb50= ggplot(data = bootstrap_b50) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 100 \noriginal_sample = rbinom(n = n, size = 50, prob = (5-sqrt(21))/2)\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\nbootstrap_b100 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrapb100= ggplot(data = bootstrap_b100) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 500 \noriginal_sample = rbinom(n = n, size = 50, prob = (5-sqrt(21))/2)\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\nbootstrap_b500 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrapb500= ggplot(data = bootstrap_b500) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\nbootstrapb20 + bootstrapb50 \nbootstrapb100 + bootstrapb500\n```\n\n::: {.cell-output-display}\n![Density plots of the variance and the variance *(n-1)/n of the Binomial distribution, for values of n 20, 50, 100, and 500 in ascending order.](Lab11WriteUp_files/figure-pdf/fig-density-binom-s7272-1.pdf){#fig-density-binom-s7272-1 fig-align='center' fig-pos='H'}\n:::\n\n::: {.cell-output-display}\n![Density plots of the variance and the variance *(n-1)/n of the Binomial distribution, for values of n 20, 50, 100, and 500 in ascending order.](Lab11WriteUp_files/figure-pdf/fig-density-binom-s7272-2.pdf){#fig-density-binom-s7272-2 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\nHere, we change the seed and observe any potential differences. \n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(97)\nn = 20   \noriginal_sample = rbinom(n = n, size = 50, prob = (5-sqrt(21))/2)\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\n\nbootstrap_b20 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrapb20= ggplot(data = bootstrap_b20) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 50 \noriginal_sample = rbinom(n = n, size = 50, prob = (5-sqrt(21))/2)\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\nbootstrap_b50 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrapb50= ggplot(data = bootstrap_b50) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 100 \noriginal_sample = rbinom(n = n, size = 50, prob = (5-sqrt(21))/2)\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\nbootstrap_b100 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrapb100= ggplot(data = bootstrap_b100) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 500 \noriginal_sample = rbinom(n = n, size = 50, prob = (5-sqrt(21))/2)\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\nbootstrap_b500 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrapb500= ggplot(data = bootstrap_b500) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\nbootstrapb20 + bootstrapb50 \nbootstrapb100 + bootstrapb500\n```\n\n::: {.cell-output-display}\n![Density plots of the variance and the variance *(n-1)/n of the Binomial distribution, for values of n 20, 50, 100, and 500 in ascending order.](Lab11WriteUp_files/figure-pdf/fig-density-binom-s979-1.pdf){#fig-density-binom-s979-1 fig-align='center' fig-pos='H'}\n:::\n\n::: {.cell-output-display}\n![Density plots of the variance and the variance *(n-1)/n of the Binomial distribution, for values of n 20, 50, 100, and 500 in ascending order.](Lab11WriteUp_files/figure-pdf/fig-density-binom-s979-2.pdf){#fig-density-binom-s979-2 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\nHere, in the Binomial distribution, we observe a similar phenomenon to the Poisson distribution. For different seeds, the graphs look different, but both seeds have $(s_n)^2$ becoming very similar to the variance as n increases.\n\nNext, we perform the same thing for the Normal distribution.\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(7272)\nn = 20   \noriginal_sample = rnorm(n = n, mean = 0, sd = sqrt(2))\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\n\nbootstrap_n20 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrapn20= ggplot(data = bootstrap_n20) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 50 \noriginal_sample = rnorm(n = n, mean = 0, sd = sqrt(2))\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\nbootstrap_n50 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrapn50= ggplot(data = bootstrap_n50) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 100 \noriginal_sample = rnorm(n=n, mean = 0, sd=sqrt(2))\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\nbootstrap_n100 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrapn100= ggplot(data = bootstrap_n100) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 500 \noriginal_sample = rnorm(n = n, mean = 0, sd = sqrt(2))\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\nbootstrap_n500 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrapn500= ggplot(data = bootstrap_n500) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\nbootstrapn20 + bootstrapn50 \nbootstrapn100 + bootstrapn500\n```\n\n::: {.cell-output-display}\n![Density plots of the variance and the variance *(n-1)/n of the Normal distribution, for values of n 20, 50, 100, and 500 in ascending order.](Lab11WriteUp_files/figure-pdf/fig-density-norm-s7272-1.pdf){#fig-density-norm-s7272-1 fig-align='center' fig-pos='H'}\n:::\n\n::: {.cell-output-display}\n![Density plots of the variance and the variance *(n-1)/n of the Normal distribution, for values of n 20, 50, 100, and 500 in ascending order.](Lab11WriteUp_files/figure-pdf/fig-density-norm-s7272-2.pdf){#fig-density-norm-s7272-2 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\nNow, we change the seed to see if a similar pattern is observed. \n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(340)\nn = 20   \noriginal_sample = rnorm(n = n, mean = 0, sd = sqrt(2))\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\n\nbootstrap_n20 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrapn20= ggplot(data = bootstrap_n20) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 50 \noriginal_sample = rnorm(n = n, mean = 0, sd = sqrt(2))\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\nbootstrap_n50 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrapn50= ggplot(data = bootstrap_n50) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 100 \noriginal_sample = rnorm(n=n, mean = 0, sd=sqrt(2))\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\nbootstrap_n100 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrapn100= ggplot(data = bootstrap_n100) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 500 \noriginal_sample = rnorm(n = n, mean = 0, sd = sqrt(2))\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\nbootstrap_n500 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrapn500= ggplot(data = bootstrap_n500) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\nbootstrapn20 + bootstrapn50 \nbootstrapn100 + bootstrapn500\n```\n\n::: {.cell-output-display}\n![Density plots of the variance and the variance *(n-1)/n of the Normal distribution, for values of n 20, 50, 100, and 500 in ascending order.](Lab11WriteUp_files/figure-pdf/fig-density-norm-s340-1.pdf){#fig-density-norm-s340-1 fig-align='center' fig-pos='H'}\n:::\n\n::: {.cell-output-display}\n![Density plots of the variance and the variance *(n-1)/n of the Normal distribution, for values of n 20, 50, 100, and 500 in ascending order.](Lab11WriteUp_files/figure-pdf/fig-density-norm-s340-2.pdf){#fig-density-norm-s340-2 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\nAgain, the variance appears to keep its shape as n increases, while $(s_n)^2$ converges to the variance as n increases.\n\nLastly, we do the same for the exponential distribution. \n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(7272)\nn = 20   \noriginal_sample = rexp(n, lambda)\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\n\nbootstrap_e20 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrape20= ggplot(data = bootstrap_e20) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 50 \noriginal_sample = rexp(n, lambda)\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\nbootstrap_e50 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrape50= ggplot(data = bootstrap_e50) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 100 \noriginal_sample = rexp(n, lambda)\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\nbootstrap_e100 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrape100= ggplot(data = bootstrap_e100) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 500 \noriginal_sample = rexp(n, lambda)\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\nbootstrap_e500 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrape500= ggplot(data = bootstrap_e500) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\nbootstrape20 + bootstrape50 \nbootstrape100 + bootstrape500\n```\n\n::: {.cell-output-display}\n![Density plots of the variance and the variance *(n-1)/n of the Exponential distribution, for values of n 20, 50, 100, and 500 in ascending order.](Lab11WriteUp_files/figure-pdf/fig-density-exp-s7272-1.pdf){#fig-density-exp-s7272-1 fig-align='center' fig-pos='H'}\n:::\n\n::: {.cell-output-display}\n![Density plots of the variance and the variance *(n-1)/n of the Exponential distribution, for values of n 20, 50, 100, and 500 in ascending order.](Lab11WriteUp_files/figure-pdf/fig-density-exp-s7272-2.pdf){#fig-density-exp-s7272-2 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\nNow, we change the seed to see if a similar pattern is observed. \n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nset.seed(582)\nn = 20   \noriginal_sample = rexp(n, lambda)\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\n\nbootstrap_e20 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrape20= ggplot(data = bootstrap_e20) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 50 \noriginal_sample = rexp(n, lambda)\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\nbootstrap_e50 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrape50= ggplot(data = bootstrap_e50) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 100 \noriginal_sample = rexp(n, lambda)\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\nbootstrap_e100 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrape100= ggplot(data = bootstrap_e100) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1) +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1) +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nn = 500 \noriginal_sample = rexp(n, lambda)\nsample_var =var(original_sample)\nnotvar = var(original_sample)*(n-1)/n\nboot_var = numeric(b)\nboot_notvar = numeric(b)\nfor (i in 1:b) {\n  resample = sample(original_sample, size = n, replace = TRUE)\n  boot_var[i] = var(resample)\n  boot_notvar[i] = var(resample)*(n-1)/n\n}\nbootstrap_e500 = tibble(bootstrap_var = boot_var, bootstrap_notvar = boot_notvar)\n```\n:::\n\n\n \\normalsize\n\n \\normalsize\n\n\n::: {.cell}\n\n```{.r .cell-code}\nbootstrape500= ggplot(data = bootstrap_e500) +\n  stat_density(aes(x = boot_var, color = \"(s_(n-1))^2\"), geom = \"line\", linewidth = 1, position = \"dodge\") +\n  stat_density(aes(x = bootstrap_notvar, color = \"(s_n)^2\"),  geom = \"line\", linewidth = 1, position = \"dodge\") +\n  scale_color_manual(values = c(\"(s_(n-1))^2\" = \"chartreuse2\",\n                                \"(s_n)^2\"  = \"darkorchid2\")) +\n  labs(x = \"Value\")\n```\n:::\n\n\n \\normalsize\n\n\n \\scriptsize\n\n\n::: {.cell layout-align=\"center\" scale='80%'}\n\n```{.r .cell-code}\nbootstrape20 + bootstrape50 \nbootstrape100 + bootstrape500\n```\n\n::: {.cell-output-display}\n![Density plots of the variance and the variance *(n-1)/n of the Exponential distribution, for values of n 20, 50, 100, and 500 in ascending order.](Lab11WriteUp_files/figure-pdf/fig-density-exp-s582-1.pdf){#fig-density-exp-s582-1 fig-align='center' fig-pos='H'}\n:::\n\n::: {.cell-output-display}\n![Density plots of the variance and the variance *(n-1)/n of the Exponential distribution, for values of n 20, 50, 100, and 500 in ascending order.](Lab11WriteUp_files/figure-pdf/fig-density-exp-s582-2.pdf){#fig-density-exp-s582-2 fig-align='center' fig-pos='H'}\n:::\n:::\n\n\n \\normalsize\n\nIn these bootstraps, we saw through the graphs that they provide roughly the same information as with the full simulation, and as n increases $s_{n-1}^2$ and $s_{n}^2$ become more similar. This was the case irrespective of seed or distribution.\n\n# Executive Summary\nSummarize the work you've done here to provide a brief (200-300 word) summary of the simulation and the results using your work in Lab 11. Your summary should be professional, clear, and all claims should be corroborated with statistical support.\n\n**Solution**\n\nIn this lab, we compared the point estimators\n$$S_{n-1}^2 = \\frac{1}{n-1} \\sum_{i=1}^n (X_i -\\bar{X})^2 $$\n\nand \n$$S_n^2 = \\frac{1}{n} \\sum_{i=1}^n (X_i -\\bar{X})^2.$$. \n\nThe goal was to determine why we use $S_{n-1}^2$ instead of $S_n^2$, by exploring the Poisson, Binomial, Normal, and Exponential distributions. The parameters of these distributions were selected such that the true variance for all models is 2. This lets us compare the estimators to the true variance, and make judgements from there. First, we began by calculating the Bias, Precision, and Mean Square Error (MSE) for $S_{n-1}^2$ and $S_n^2$ within each distribution, and then changing the sample size and observing the effect. To start, when $n=20$, $S_{n-1}^2$ has a higher MSE, lower bias, and lower precision across all distributions when compared to $S_n^2$. However, the mean of $S_{n-1}^2$ is closer to the true mean of 2 than $S_n^2$, across all distributions. As n increases, the difference in MSE, Bias, and Precision across all distributions decreases, and the mean of both distributions becomes closer to 2. When producing a density plot of $s_{n-1}^2$ and $s_{n}^2$, and doing so with increasing n, it is apparent that their distributions are becoming approximately normal as n increases. This was the case for all four of the distributions tested. When performing a bootstrap on each distribution, the bootstrap yielded very similar results to the full simulation. This was the case regardless of what seed was used. We can conclude that bootstrapping is an effective method, but its performance significantly improves when n is large (~500). ",
    "supporting": [
      "Lab11WriteUp_files/figure-pdf"
    ],
    "filters": [
      "rmarkdown/pagebreak.lua"
    ],
    "includes": {},
    "engineDependencies": {},
    "preserve": null,
    "postProcess": false
  }
}